# Manuals

Place the VisionMaster help manual here when running the RAG assistant locally:

```text
Manuals/HikRobotVMHelp.chm
```

The CHM manual is not committed to this repository because it is a third-party binary document. The application can also read a custom path from `VmManualRag.ChmPath` in `App.config`.
